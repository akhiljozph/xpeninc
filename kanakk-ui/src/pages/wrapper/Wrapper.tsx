import type { WrapperProps } from "./Wrapper.types";

import "./Wrapper.scss";

const Wrapper = (props: WrapperProps) => {
    return (
        <>
            wrapper works!
        </>
    )
}
export default Wrapper;